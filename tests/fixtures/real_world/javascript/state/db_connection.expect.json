{
  "description": "Database connection leak: queryUnsafe creates a mysql connection without calling conn.end(). querySafe properly closes.",
  "tags": [
    "state",
    "resource-leak",
    "database",
    "mysql"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "state-resource-leak",
      "severity": null,
      "must_match": true,
      "line_range": [
        2,
        11
      ],
      "evidence_contains": [],
      "notes": "mysql.createConnection without conn.end(); resource-pair rules include mysql.createConnection acquire and .end release"
    }
  ]
}
