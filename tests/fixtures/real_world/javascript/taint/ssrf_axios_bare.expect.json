{
  "description": "SSRF: user-controlled URL from req.query flows to bare axios() call",
  "tags": ["taint", "ssrf", "axios"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [5, 10],
      "evidence_contains": [],
      "notes": "req.query.url flows to axios(url) without URL validation"
    }
  ]
}
