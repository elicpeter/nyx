{
  "description": "Command injection via string concat — symex should generate witness with shell payload.",
  "tags": [
    "taint",
    "cmdi",
    "symex",
    "witness"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        5,
        7
      ],
      "evidence_contains": [],
      "notes": "req.query.cmd flows into child_process.exec via string concat; symex witness should contain shell injection payload"
    }
  ]
}
