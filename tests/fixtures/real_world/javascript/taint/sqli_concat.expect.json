{
  "description": "SQL injection via string concatenation with userId parameter. connection.query is now a recognized SQL sink, but userId is a function param (not a recognized source).",
  "tags": [
    "taint",
    "sqli",
    "mysql"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        2,
        7
      ],
      "evidence_contains": [],
      "notes": "userId flows into SQL string via concat to connection.query via handler param-as-source heuristic"
    }
  ]
}
