{
  "description": "XSS with non-global replace: name.replace('<', '&lt;') only replaces first occurrence. Taint should NOT be cleared (conservative policy).",
  "tags": [
    "taint",
    "xss",
    "symex",
    "string-ops",
    "replace",
    "phase-22"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        4,
        8
      ],
      "evidence_contains": [],
      "notes": "req.query.name flows through non-global replace into res.send — replace does not clear taint"
    }
  ]
}
