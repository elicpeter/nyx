{
  "description": "SQL injection via string concatenation into pg client.query()",
  "tags": ["taint", "sqli", "pg"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [3, 8],
      "evidence_contains": [],
      "notes": "req.query.id flows into SQL string via concat, then to client.query()"
    }
  ]
}
