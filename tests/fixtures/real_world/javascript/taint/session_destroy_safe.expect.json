{
  "description": "Safe session destroy — no user-controlled data flows to any sink",
  "tags": ["taint", "express", "safe"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "must_not_match": true,
      "line_range": [4, 8],
      "notes": "req.session.destroy with static callback and /login redirect must NOT fire (regression guard)"
    }
  ]
}
