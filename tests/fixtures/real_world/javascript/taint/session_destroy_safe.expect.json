{
  "description": "Safe session destroy — no user-controlled data flows to any sink",
  "tags": ["taint", "express", "safe"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": false,
      "line_range": [4, 8],
      "evidence_contains": [],
      "notes": "req.session.destroy with static callback and /login redirect should not fire"
    }
  ]
}
