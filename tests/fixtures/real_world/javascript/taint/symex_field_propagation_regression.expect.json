{
  "description": "Field propagation regression guard: taint flows through field even after parseInt transformation.",
  "tags": ["taint", "field-sensitivity", "sqli", "regression"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": false,
      "line_range": [4, 9],
      "evidence_contains": [],
      "notes": "req.query.name flows through parseInt to user.id field, then to SQL query — taint-level flow is ground truth; parseInt suppression is via type-safety, not symex"
    }
  ]
}
