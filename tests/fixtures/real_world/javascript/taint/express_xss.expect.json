{
  "description": "XSS via req.query flowing into res.send. DOMPurify.sanitize strips HTML_ESCAPE cap.",
  "tags": ["taint", "xss", "express", "res.send"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [3, 7],
      "evidence_contains": [],
      "notes": "req.query.name flows directly into res.send via string concatenation — XSS"
    },
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": false,
      "line_range": [9, 13],
      "evidence_contains": [],
      "notes": "DOMPurify.sanitize strips HTML_ESCAPE cap so res.send should NOT fire"
    }
  ]
}
