{
  "description": "SQL injection via string concat — symex should generate witness with SQL payload.",
  "tags": [
    "taint",
    "sqli",
    "symex",
    "witness"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        4,
        9
      ],
      "evidence_contains": [],
      "notes": "req.query.id flows into db.query via string concat; symex witness should contain SQL injection payload"
    }
  ]
}
