{
  "description": "Session destroy with unrelated req.query access — no flow to sink",
  "tags": ["taint", "express", "safe"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": false,
      "line_range": [4, 9],
      "evidence_contains": [],
      "notes": "req.query.next is assigned to returnTo but never flows to a sink — session.destroy callback has static redirect"
    }
  ]
}
