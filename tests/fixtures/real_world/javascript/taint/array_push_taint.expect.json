{
  "description": "XSS through array operations: req.query.item pushed into array then joined into HTML",
  "tags": [
    "taint",
    "xss",
    "express",
    "array",
    "container"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        3,
        8
      ],
      "evidence_contains": [],
      "notes": "array push + join taint tracked via container propagation"
    }
  ]
}
