{
  "description": "Taint propagation through variable reassignment chain: req.query.input -> x -> y -> z -> eval.",
  "tags": ["taint", "cmdi", "express", "eval", "reassignment"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [3, 9],
      "evidence_contains": [],
      "notes": "req.query.input flows through reassignment chain x -> y -> z into eval"
    }
  ]
}
