{
  "description": "got({url: taintedUrl}) — destination field is attacker-controlled. Must fire SSRF.",
  "tags": ["taint", "ssrf", "got", "destination-aware", "object-config"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [5, 11],
      "evidence_contains": [],
      "notes": "req.query.target → got({url: target, ...}) — tainted destination field."
    }
  ]
}
