{
  "description": "Multi-path symbolic exploration: one branch assigns a constant (safe), the other carries taint to eval. Tests that the multi-path engine explores both branches and confirms the finding via the tainted path.",
  "tags": [
    "taint",
    "code-exec",
    "symex",
    "multipath"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        4,
        13
      ],
      "evidence_contains": [],
      "notes": "req.query.payload flows through else-branch into eval; multi-path engine confirms via feasible tainted path"
    }
  ]
}
