{
  "description": "Field write taints base object — obj.data = source(); sink(obj) should fire.",
  "tags": ["taint", "xss", "field-access"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [4, 7],
      "evidence_contains": [],
      "notes": "obj.data = req.query.input taints obj via synthetic base update; res.send(obj) should detect tainted base"
    }
  ]
}
