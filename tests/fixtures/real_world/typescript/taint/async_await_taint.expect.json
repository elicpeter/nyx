{
  "description": "SSRF via req.query.url flowing into fetch through async/await.",
  "tags": ["taint", "ssrf", "express", "fetch", "async-await"],
  "modes": ["full"],
  "strict_unexpected": ["taint-unsanitised-flow"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [3, 9],
      "evidence_contains": [],
      "notes": "req.query.url flows into fetch via async/await — SSRF"
    },
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [8, 8],
      "evidence_contains": [],
      "notes": "fetched response body flows to res.send — secondary XSS-like response flow via SSRF-fetched content"
    }
  ]
}
