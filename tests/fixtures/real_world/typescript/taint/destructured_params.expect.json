{
  "description": "SSRF via req.query.host flowing into fetch URL construction",
  "tags": [
    "taint",
    "ssrf",
    "express",
    "typescript",
    "fetch"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        3,
        7
      ],
      "evidence_contains": [],
      "notes": "req.query.host flows into fetch URL via string concatenation (SSRF)"
    }
  ]
}
