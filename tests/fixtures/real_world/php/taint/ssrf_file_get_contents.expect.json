{
  "description": "SSRF: user-controlled URL from $_GET flows to file_get_contents() (dual Source+Sink label)",
  "tags": ["taint", "ssrf", "multi-label"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [1, 7],
      "evidence_contains": [],
      "notes": "$_GET['url'] flows to file_get_contents($url) — file_get_contents has dual Source+Sink(SSRF) labels"
    }
  ]
}
