{
  "description": "PHP infeasible nested equality: $action === 'safe' AND $action === 'dangerous' is contradictory.",
  "tags": ["taint", "constraint", "infeasible", "equality", "php"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [2, 9],
      "evidence_contains": [],
      "notes": "$_GET['action'] flows to outer system — reachable path"
    }
  ]
}
