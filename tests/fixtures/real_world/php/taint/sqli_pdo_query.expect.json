{
  "description": "SQL injection via PDO::query() with string concatenation",
  "tags": ["taint", "sqli", "pdo", "php"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [2, 5],
      "evidence_contains": [],
      "notes": "$_GET['name'] flows into SQL string via concat, then to pdo->query()"
    }
  ]
}
