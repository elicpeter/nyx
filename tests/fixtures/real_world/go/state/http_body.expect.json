{
  "description": "HTTP response body leak in fetchLeak; fetchSafe uses defer resp.Body.Close()",
  "tags": [
    "state",
    "resource-leak",
    "http"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "state-resource-leak",
      "severity": null,
      "must_match": false,
      "line_range": [
        7,
        14
      ],
      "evidence_contains": [],
      "notes": "resp.Body from http.Get never closed in fetchLeak. Deferred: Go CallFn does not extract receiver into uses, so resp.Body.Close() cannot match acquired resp symbol."
    }
  ]
}
