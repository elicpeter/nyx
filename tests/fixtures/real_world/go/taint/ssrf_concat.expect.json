{
  "description": "SSRF positive: env var concatenated into URL and passed to http.Get()",
  "tags": [
    "taint",
    "ssrf",
    "concat"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        1,
        12
      ],
      "evidence_contains": [],
      "notes": "os.Getenv(\"TARGET_HOST\") -> host -> url (concatenation) -> http.Get(url)"
    }
  ]
}
