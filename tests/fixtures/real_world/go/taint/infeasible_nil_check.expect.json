{
  "description": "Infeasible nested equality: cmd == 'safe' AND cmd == 'rm' is contradictory. Only the outer exec.Command should produce a finding.",
  "tags": ["taint", "constraint", "infeasible", "equality"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [9, 17],
      "evidence_contains": [],
      "notes": "r.URL.Query().Get flows to outer exec.Command — reachable path"
    }
  ]
}
