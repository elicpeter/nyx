{
  "description": "getenv source flows through sanitize_data (HTML_ESCAPE sanitizer) before reaching strcpy sink — taint should be cleared.",
  "tags": ["taint", "negative", "buffer", "sanitizer"],
  "modes": ["full"],
  "expected": []
}
