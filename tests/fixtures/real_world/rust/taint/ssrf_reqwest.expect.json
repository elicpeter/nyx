{
  "description": "SSRF: environment variable flows to reqwest::get()",
  "tags": ["taint", "ssrf", "reqwest"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [3, 6],
      "evidence_contains": [],
      "notes": "env::var(\"TARGET_URL\") flows to reqwest::get(&url) without URL validation"
    },
    {
      "rule_id": "rs.quality.unwrap",
      "severity": null,
      "must_match": true,
      "line_range": [3, 6],
      "evidence_contains": [],
      "notes": ".unwrap() on env::var Result"
    }
  ]
}
