{
  "description": "Regression guard: when a replacement literal itself contains `..` (a dangerous path-traversal sequence), the scanner must abandon all sanitizer credit for the whole replace chain.",
  "tags": ["taint", "positive", "path-traversal", "regression-guard", "replace-chain"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [11, 15],
      "evidence_contains": [],
      "notes": "Replacement literal reintroduces `..`; sanitization cannot be trusted."
    }
  ]
}
