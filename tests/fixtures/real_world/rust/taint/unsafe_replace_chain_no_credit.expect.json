{
  "description": "Regression guard: a `.replace` chain whose literals do NOT contain a known-dangerous sequence (no `..`, `/`, `\\`, `<`, `>`) must not earn sanitizer credit.  The taint path from env::var to fs::read_to_string must still be flagged.",
  "tags": ["taint", "positive", "path-traversal", "regression-guard", "replace-chain"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [11, 15],
      "evidence_contains": [],
      "notes": "Replace chain on unrelated characters does not sanitize path-traversal."
    }
  ]
}
