{
  "description": "SQL injection via SQLAlchemy session.execute() with string concatenation",
  "tags": ["taint", "sqli", "sqlalchemy", "python"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [5, 10],
      "evidence_contains": [],
      "notes": "os.environ flows into SQL string via concat, then to session.execute()"
    }
  ]
}
