{
  "description": "SQL injection via sqlite3 conn.execute() with string concatenation",
  "tags": ["taint", "sqli", "sqlite3", "python"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [4, 8],
      "evidence_contains": [],
      "notes": "os.environ flows into SQL string via concat, then to conn.execute()"
    }
  ]
}
