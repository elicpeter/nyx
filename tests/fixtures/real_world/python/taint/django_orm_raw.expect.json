{
  "description": "Django ORM raw SQL injection: request.GET flows to User.objects.raw()",
  "tags": ["taint", "sqli", "django"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [4, 6],
      "evidence_contains": [],
      "notes": "request.GET.get('q') flows to User.objects.raw() — SQL injection via Django ORM"
    }
  ]
}
