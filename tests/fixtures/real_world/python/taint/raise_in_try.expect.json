{
  "description": "Python raise→except exception edge: taint flows to os.system in try body after conditional raise",
  "tags": [
    "taint",
    "cmdi",
    "raise",
    "try_except",
    "python"
  ],
  "modes": [
    "full"
  ],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [
        6,
        6
      ],
      "evidence_contains": [],
      "notes": "input() flows to os.system(val) in try body"
    },
    {
      "rule_id": "py.cmdi.os_system",
      "severity": "MEDIUM",
      "must_not_match": true,
      "line_range": [
        6,
        6
      ],
      "notes": "Layer B suppression: AST pattern must NOT fire when taint finding at same sink confirms (regression guard)"
    },
    {
      "rule_id": "cfg-error-fallthrough",
      "severity": "LOW",
      "must_match": false,
      "line_range": [
        4,
        4
      ],
      "evidence_contains": [],
      "notes": "noise: error check fallthrough detection at if-not-val guard"
    }
  ]
}
