{
  "description": "SSRF safe (Python): URL has known-safe prefix with scheme + host. Cross-language prefix suppression.",
  "tags": ["taint", "ssrf", "abstract-interp", "string-domain"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": false,
      "line_range": [7, 8],
      "evidence_contains": [],
      "notes": "TN: URL prefix 'https://api.internal.example.com/users/' locks the host — SSRF suppressed"
    }
  ]
}
