{
  "description": "SQL injection: user-controlled params[:q] flows to find_by_sql raw query",
  "tags": ["taint", "sqli", "rails", "ruby"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [2, 5],
      "evidence_contains": [],
      "notes": "params[:q] flows into find_by_sql string interpolation without sanitization"
    }
  ]
}
