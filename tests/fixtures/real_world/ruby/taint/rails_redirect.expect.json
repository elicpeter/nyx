{
  "description": "Open redirect: user-controlled params[:redirect_url] flows to redirect_to",
  "tags": ["taint", "redirect", "rails", "ruby"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [2, 5],
      "evidence_contains": [],
      "notes": "params[:redirect_url] flows to redirect_to without validation"
    },
    {
      "rule_id": "cfg-unguarded-sink",
      "severity": "MEDIUM",
      "must_match": false,
      "line_range": [7, 9],
      "evidence_contains": [],
      "notes": "noise: redirect_to root_path in safe_login — root_path is a Rails helper, not user input; constant-arg check can't trace framework helpers"
    }
  ]
}
