{
  "description": "Taint flows through begin/rescue — gets flows to system() inside try body",
  "tags": ["taint", "cmdi", "begin-rescue", "ruby"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [2, 8],
      "evidence_contains": [],
      "notes": "gets flows to system(input) inside begin block"
    }
  ]
}
