{
  "description": "SQL injection: user-controlled params[:name] interpolated into where() raw SQL string",
  "tags": ["taint", "sqli", "rails", "ruby", "activerecord"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [2, 5],
      "evidence_contains": [],
      "notes": "params[:name] flows into where() string interpolation — classic Rails SQLi"
    }
  ]
}
