{
  "description": "Sinatra XSS: params flows to erb() (requires Sinatra framework detection via Gemfile)",
  "tags": ["taint", "xss", "sinatra", "framework"],
  "modes": ["full"],
  "expected": [
    {
      "rule_id": "taint-unsanitised-flow",
      "severity": null,
      "must_match": true,
      "line_range": [3, 6],
      "evidence_contains": [],
      "notes": "params[:name] flows to erb() — Sinatra framework detected from in-file `require 'sinatra'`"
    }
  ]
}
